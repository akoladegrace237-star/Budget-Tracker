[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python -c 'from db import init_db; init_db()' && gunicorn app:app --bind 0.0.0.0:$PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
